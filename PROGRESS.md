# PROGRESS.md — GDG Quantum (Completion Log)

Append-only record of completed work. CLAUDE.md is the source of truth for the plan;
this file is the reference for what has been done. Newest entries at the bottom of each phase.

---

## Phase 0 — Foundation & design system
- [x] Scaffold Next.js 14 + TS strict + Tailwind + ESLint/Prettier. — `@frontend-engineer` — 2026-06-09
  - Files: package.json, tsconfig.json, next.config.mjs, postcss.config.mjs, tailwind.config.ts, .eslintrc.json, .prettierrc.json, .prettierignore, .gitignore
  - Summary: Hand-scaffolded Next.js 14.2.35 (App Router) with TypeScript `strict: true`, Tailwind 3.4, ESLint (next/core-web-vitals + prettier) and Prettier (+tailwind plugin). Built manually rather than create-next-app to avoid prompts conflicting with existing CLAUDE.md/.claude.
  - Verify: `npm install` then `npm run lint` (clean), `npx tsc --noEmit` (exit 0), `npm run build` (exit 0)
  - QA: PASS
- [x] Wire design tokens into Tailwind + CSS vars; self-host fonts via `next/font`. — `@frontend-engineer` — 2026-06-09
  - Files: app/globals.css, tailwind.config.ts, lib/fonts.ts, app/layout.tsx
  - Summary: Tokens (--bg/--ink/--accent/--muted/--hairline) defined once as CSS vars in globals.css and mapped into Tailwind theme.extend colors via var(). Inter (variable) + Instrument Serif (400) self-hosted via next/font/google with size-adjusted fallback (adjustFontFallback default) to prevent CLS; exposed as --font-inter/--font-serif on <html>.
  - Verify: `npm run build`; `<html>` carries two next/font `__variable_*` classes; compiled CSS references `var(--font-inter)`; headings render serif, body Inter
  - QA: PASS
- [x] Establish folder structure; commit `.env.example` skeleton. — `@frontend-engineer` — 2026-06-09
  - Files: app/, components/ui/.gitkeep, components/sections/.gitkeep, lib/, prisma/.gitkeep, .env.example
  - Summary: Created §2 folder layout (app, components/ui, components/sections, lib, prisma) with .gitkeep stubs for empty dirs. Added .env.example with keys-only placeholders (DATABASE_URL + DIRECT_URL, Resend/SMTP, Turnstile, Upstash, IP_HASH_SALT) — no secrets, nothing sensitive under NEXT_PUBLIC_.
  - Verify: `ls -1a components/ui components/sections prisma` shows .gitkeep; `.env.example` present at root
  - QA: PASS
- [x] Add a single `siteConfig` (panel labels incl. the swappable "Systems"). — `@frontend-engineer` — 2026-06-09
  - Files: lib/site-config.ts
  - Summary: Single siteConfig with brand "GDG QUANTUM" and the four panels (01 Landing #home, 02 About #about, 03 Systems #systems, 04 Contact #contact). The Systems label is a standalone `SYSTEMS_LABEL` constant so it can be swapped in one place. Typed with a Panel interface; consumed by layout metadata + temp test page.
  - Verify: `npx tsc --noEmit` clean; siteConfig imported in app/layout.tsx and app/page.tsx
  - QA: PASS

### ✅ GATE APPROVED — Phase 0 — approved by Gabe — 2026-06-09
- QA result: PASS
- Notes: Foundation & design system verified (build/lint/tsc clean, dev boots 200, tokens+fonts render, siteConfig + .env.example correct). Orchestration runs in main session (subagents can't spawn subagents in this harness); QA performed by an independent general-purpose agent in lieu of a missing `qa-reviewer.md`. `app/page.tsx` is a temporary tokens/fonts smoke-test to be replaced in Phase 1.

## Phase 1 — Static panel shell + nav + preloader
- [x] Build the four panels as static, full-viewport (`100svh`) sections with real copy/layout (no snap yet). — `@frontend-engineer` — 2026-06-09
  - Files: app/page.tsx (replaced Phase 0 smoke test), components/sections/Panel.tsx, components/sections/Landing.tsx, components/sections/About.tsx, components/sections/Systems.tsx, components/sections/Contact.tsx, components/ui/CtaLink.tsx, components/ui/Field.tsx, app/globals.css, app/layout.tsx
  - Summary: Built shared `Panel` wrapper (min-h-[100svh], landmark `<section aria-label>`, decorative index marker) and the four panels with restrained placeholder copy: 01 Landing (#home) holds the single `<h1>` + sub + accent CTA (hand-built `CtaLink`) + scroll cue + a clearly-marked STATIC hero-object placeholder for Phase 3; 02 About (#about) philosophy statement; 03 Systems (#systems, via `SYSTEMS_LABEL`) intro + 4 capability items; 04 Contact (#contact) visual-only labelled form (`Field` primitive, submit disabled) + footer. Panels stack for native scroll — no GSAP/snap and no entrance animations yet. Added skip-link + a `prefers-reduced-motion` guard (kills smooth-scroll/transitions) in globals.css/layout.
  - Verify: `npm run lint` (clean), `npx tsc --noEmit` (exit 0), `npm run build` (exit 0, `/` prerenders as static, 97.8 kB First Load JS); `npm run dev` serves 200 with all four panel ids, exactly one `<h1>`, and all section headings present
  - QA: PASS
- [x] Top bar (hex mark + `GDG QUANTUM` + nav links). — `@frontend-engineer` — 2026-06-09
  - Files: components/chrome/TopBar.tsx, app/page.tsx
  - Summary: Fixed `<header>` with an inline-SVG hex mark + `GDG QUANTUM` wordmark (server component) and a `<nav aria-label="Primary">` of anchor links to the four hashes. Inline nav links hide below `md` (numbered SectionNav handles mobile wayfinding); wordmark always visible. Visible focus rings via `focus-visible`. Clicks are plain hash anchors this phase; Phase 2 routes them through `goToPanel()`.
  - Verify: rendered HTML contains `aria-label="Primary"`, the hex `<svg>`, and four `#`-hash links; visible at desktop, links collapse below md
  - QA: PASS
- [x] Numbered section nav (01–04) + progress indicator (visual only for now). — `@frontend-engineer` — 2026-06-09
  - Files: components/chrome/SectionNav.tsx, app/page.tsx
  - Summary: Fixed right-rail `<nav aria-label="Sections">` (client component) listing 01 Landing · 02 About · 03 Systems · 04 Contact with a hover/active hairline-to-ink treatment and a vertical accent progress indicator. Reflects the URL hash via a `hashchange` listener and sets `aria-current` on the active item — purely a visual treatment this phase, NOT wired to a snap engine (Phase 2 swaps the hash-reading for the Observer deck's active index). Shown from `lg` up; hidden on smaller screens.
  - Verify: rendered HTML contains `aria-label="Sections"` and the four indexed labels; active item updates on hash change in-browser
  - QA: PASS
- [x] Preloader overlay with real-progress percentage counter (fonts + key assets), tasteful min duration. — `@frontend-engineer` — 2026-06-09
  - Files: components/chrome/Preloader.tsx, app/page.tsx
  - Summary: `"use client"` full-screen overlay counting 0→100 driven by REAL load signals — `document.fonts.ready` (CLS-critical: both faces ready before the hero headline paints) and the window `load` event, each contributing half the target; the displayed number eases toward the target on rAF so it always advances. Guards: MIN_DURATION 1100ms (no flash on cached loads), hard MAX_DURATION 6000ms fail-safe (never traps if a signal never fires), body-scroll lock while visible (released on reveal), and a `prefers-reduced-motion` path that resolves near-instantly with no fade. Marked `role="status"`/`aria-live` with an `sr-only` percentage for screen readers.
  - Verify: `npm run dev` → overlay shows brand + counting number then clears to the hero; rendered HTML contains `role="status"` and "Loading"; with OS reduce-motion on, overlay resolves immediately
  - QA: PASS

### ✅ GATE APPROVED — Phase 1 — approved by Gabe — 2026-06-09
- QA result: PASS
- Notes: Four static 100svh panels + top bar + numbered section nav + real-progress preloader verified (lint/tsc/build clean, dev 200, one h1, landmarks, labelled fields, tokens-only, reduced-motion guards, no out-of-scope GSAP/snap). SectionNav active state is hash-only by design — Phase 2 Observer deck takes over active-index tracking. Placeholder copy is invented/restrained and swappable; Gabe reviewed at the gate.

## Phase 2 — GSAP snap engine
- [x] Implement the `Observer` panel deck: `goToPanel(index)` as the single entry point for wheel/touch, keyboard (`Arrow/PageUp/Down/Home/End`), nav clicks, and hash deep-links. — `@frontend-engineer` — 2026-06-09
  - Files: package.json (+gsap@3.12.5), components/deck/DeckContext.tsx (new), components/deck/PanelDeck.tsx (new), app/page.tsx, components/chrome/TopBar.tsx, components/chrome/SectionNav.tsx, components/sections/Panel.tsx
  - Summary: Built a `"use client"` GSAP `Observer` panel deck. `PanelDeck` owns the engine and exposes the SINGLE entry point `goToPanel(index)` via `DeckContext`. In deck mode it translates a track (`yPercent: -100*index`, power2.out, 0.9s) — one gesture = one panel. Inputs funnel through goToPanel: Observer (`type:"wheel,touch,pointer"`, onUp→next / onDown→prev, tolerance 10, preventDefault); keyboard (Arrow/Page/Home/End/Space, suppressed in form fields); TopBar + SectionNav clicks (intercept anchor, call goToPanel, render real active index + `aria-current`); hash deep-links (initial `location.hash` sets start panel via `gsap.set`; `replaceState` updates hash on change; `hashchange`/`popstate` route back through goToPanel).
  - Verify: `npm run lint` (clean), `npx tsc --noEmit` (exit 0), `npm run build` (exit 0, `/` static, 130 kB First Load JS), `npm run dev` serves 200; rendered HTML has one h1, main#content, four panel ids + tabindex=-1, Primary + Sections nav. No `scroll-snap-type` in source or built CSS.
  - QA: PASS
- [x] Input lock during transitions (no double-skip); `100svh` handling; ScrollTrigger refresh on resize. — `@frontend-engineer` — 2026-06-09
  - Files: components/deck/PanelDeck.tsx, components/sections/Panel.tsx
  - Summary: `lockedRef` set true for the transition duration; goToPanel early-returns while locked, so a fast double wheel/swipe can't skip two panels. Lock clears in the tween `onComplete` (focus moves to the panel then). Movement clamped to [0, panelCount-1] (NO wrap). Panels are `h-[100svh]` so one `yPercent:-100` step == exactly one viewport; yPercent is unit-relative so it survives viewport resizes. No ScrollTrigger is used (deck is Observer-driven, not scroll-driven) — instead a rAF-debounced `resize`/`orientationchange` handler re-pins the transform and rebuilds the engine if the mode crosses the mobile breakpoint or reduce-motion toggles. React 18 strict/dev double-init guarded: single mount effect, `gsap.context` + `Observer.kill()` + listener removal on cleanup (`ctx.revert()` clears inline transforms).
  - Verify: same build/lint/tsc as above; manual: fast repeated wheel ticks advance one panel each, never two; resize keeps the active panel pinned; no duplicate Observers after Fast-Refresh.
  - QA: PASS
- [x] `prefers-reduced-motion` fallback to native scroll. — `@frontend-engineer` — 2026-06-09
  - Files: components/deck/PanelDeck.tsx, components/chrome/TopBar.tsx, components/chrome/SectionNav.tsx
  - Summary: When `prefers-reduced-motion: reduce` is set the Observer deck is DISABLED entirely (no Observer, no transforms, no smoothing): the deck root becomes inert (`relative`), panels flow in normal document order, and the page uses NATIVE scroll. goToPanel falls back to an instant `scrollIntoView({behavior:'auto'})` — so nav clicks + deep-links still land — with no input lock so nothing can trap. A live `matchMedia('(prefers-reduced-motion: reduce)')` `change` listener rebuilds into/out of this mode without a reload. Complements the existing globals.css reduce-motion guard.
  - Verify: OS "reduce motion" on → page scrolls natively, no snapping; nav/deep-link jump instantly; nothing trapped. (Real-device confirm at the gate.)
  - QA: PASS
- [x] Mobile strategy: tuned Observer **or** documented fallback below ~768px. Document the choice. — `@frontend-engineer` — 2026-06-09
  - Files: components/deck/PanelDeck.tsx (MOBILE_BP + mode logic + header comment)
  - Summary: DECISION = native-scroll fallback below 768px (NOT a touch Observer deck). Rationale: a transform-based deck fights iOS Safari's dynamic toolbar and touch momentum — exactly where touch traps / double-skips appear — whereas native vertical scroll is robust and well-understood on small screens. goToPanel still works on mobile (instant `scrollIntoView`) so TopBar/SectionNav clicks and `/#systems` deep-links land correctly; the active-index/`aria-current` sync still applies. The deck-vs-native decision is recomputed on resize/orientation so crossing the breakpoint (e.g. rotating a tablet) rebuilds cleanly. Documented in code header comment, this entry, and the PM report.
  - Verify: resize below 768px → native scroll, panels flow; nav clicks jump to the right panel; no touch trap. (iOS Safari + Android Chrome are Gabe gate items.)
  - QA: PASS

### Fix — track-translate bug (Gate 2 re-completion)
- [x] FIX: deck track translation overshoot — `@frontend-engineer` — 2026-06-09
  - Files: components/deck/PanelDeck.tsx (goToPanel tween + initial gsap.set + resize re-pin)
  - Summary: Gate 2 was failed by Gabe because the track translated a full track-height per step (old `yPercent: -100 * index`), overshooting the deck so panels skipped past the viewport and panel 04 left empty space. Fixed to `yPercent: -(100 / panelCount) * index` (= −25% per panel for the 4-panel deck). Since GSAP `yPercent` is relative to the track's own height (panelCount × 100svh), each index step now moves exactly one viewport (100svh), and panel 04 (Contact) lands flush at the viewport bottom with no gap. Applied consistently in the goToPanel tween, the initial deep-link `gsap.set`, and the resize re-pin.
  - Verify: `npm run build` exit 0 (clean; `/` static, 130 kB First Load JS) after killing the stray dev server and deleting `.next`; lint + `tsc --noEmit` already green. Transform-math QA: track = 4 × 100svh = 400svh; yPercent −25·i ⇒ translateY −100svh·i (one viewport/step); index 3 ⇒ −300svh ⇒ Contact fills the final 100svh slice flush to viewport bottom, no empty space.
  - QA: PASS

### Fix — CTA bypassed the deck (Gate 2 re-completion)
- [x] FIX: CtaLink routed through goToPanel (single entry point) — `@frontend-engineer` — 2026-06-09
  - Files: components/ui/CtaLink.tsx
  - Summary: Gate 2 re-test failed: the Landing "Start a project" CTA (`<CtaLink href="#contact">`) was a plain `next/link` and bypassed the deck — unlike TopBar/SectionNav which consume `useDeck()` and call `goToPanel()`. Next's router did a native `scrollIntoView` on the Contact panel inside the `fixed inset-0 overflow-hidden` deck root (overflow:hidden is still programmatically scrollable), so Contact appeared with no slide, `activeIndex` stayed 0 (SectionNav stuck on 01), and the stale root `scrollTop` compounded with later track tweens → nav clicks landed on white space. Fix: made CtaLink `'use client'`, consuming `useDeck()`; on click it resolves the target index from `siteConfig.panels` (match `panel.hash` === href minus leading `#`), and when a deck exists and the href is a panel hash it `preventDefault()`s and calls `deck.goToPanel(index)`. Graceful fallback preserved: no deck (pre-mount/no provider) or non-panel href → native `<Link>` behaves as before; href stays on the element (real, deep-linkable, keyboard/SR-friendly). Composes a caller-supplied `onClick` (called first; bails if it `preventDefault`s) rather than clobbering, and keeps all styling/markup + forwarded `...rest`/`className`.
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0 (`/` static, 131 kB First Load JS) after stopping the stray `next dev` and deleting `.next`. Symptom trace against fixed code: (a) CTA now calls goToPanel → track tween animates one transition into Contact (no native scroll jump); (b) goToPanel updates activeIndex → SectionNav indicator moves to 04; (c) no native scrollIntoView, so no stale root scrollTop → subsequent nav clicks land on real panel content, no white space.
  - QA: PASS — independent QA re-read corrected `CtaLink.tsx` (panelIndex resolves `#contact`→3; `onClick` destructured so `{...rest}` can't clobber `handleClick`; composed-onClick precedence correct; native `<Link>` fallback intact) and independently re-ran `npm run build` → exit 0, compiled successfully, Next lint+type-check passed.

### ✅ GATE APPROVED — Phase 2 — approved by Gabe — 2026-06-09
- QA result: PASS
- Notes: GSAP Observer panel deck verified after two Gate-2 re-completions. (1) Track-translate overshoot fixed to `yPercent: -(100/panelCount)*index` (one viewport per step; panel 04 flush at bottom, no empty space). (2) Landing "Start a project" CTA had bypassed the deck via raw `next/link` — root-caused to the three symptoms Gabe reported (no-slide jump into Contact, indicator stuck on 01, white space on later nav clicks from a stale `overflow-hidden` root `scrollTop`); fixed by routing CtaLink through `useDeck()`→`goToPanel()`, mirroring TopBar/SectionNav. Build exit 0, lint+tsc clean throughout. Mobile/reduced-motion native-scroll fallback and real-device checks were part of the gate. Phase 3 (hero + entrance/reveal animations) is next.

---

## Phase 3 — Hero + entrance & reveal animations
- [x] Hero glass object — R3F "Apple liquid glass" (APPROVED SCOPE CHANGE: Spline/R3F explicitly approved by Gabe, replacing the Tier-1 next/image render) — `@frontend-engineer` — 2026-06-10
  - Deps added: `three@0.169.0`, `@react-three/fiber@8.18.0` (React-18 line; v9 needs React 19), `@react-three/drei@9.122.0` (fiber-v8 line; v10 needs fiber v9), `@types/three@0.169.0` (dev). Also bumped `gsap ^3.12.5 → 3.15.0` — the version that ships the now-free SplitText on npm (3.12.5 didn't include it).
  - Files: components/hero/HeroObject.tsx (new, swappable seam), components/hero/HeroGlass.tsx (new, WebGL chunk), components/hero/HeroObjectFallback.tsx (new, static SVG), lib/webgl.ts (new), lib/use-reduced-motion.ts (new), components/sections/Landing.tsx, package.json, package-lock.json
  - Summary: Single refractive glass orb via drei `MeshTransmissionMaterial` (ior 1.45, low roughness, chromatic aberration + temporal distortion for the "liquid" drift), lit by a LOCAL `Environment` of `Lightformer`s (no external HDR fetch — keeps the Phase 5 CSP allowlist clean + env buffer cheap, resolution 256), grounded by `ContactShadows`. dpr capped `[1,2]`, samples 6. `HeroObject` is the stable seam Landing imports: it always renders the static `HeroObjectFallback` (SSR/first-paint, no-WebGL, reduced-motion) and only `dynamic(import, { ssr: false })`s the canvas when reduced-motion is OFF and `isWebGLAvailable()`. WebGL stack confirmed split into a ~668 kB async chunk, OUT of the route's 137 kB First Load JS. The <h1> stays LCP (canvas mounts after paint, crossfades over the fallback). In-canvas slow float/rotation + pointer-parallax tilt via `useFrame`; parallax disabled on coarse/touch pointers.
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0 (`/` 137 kB First Load JS; three.js in a separate ~668 kB async chunk). Toggle OS reduce-motion → static SVG orb, no canvas. Disable WebGL → static SVG orb.
  - QA: PASS — independent QA read HeroObject.tsx (correct `dynamic(import,{ssr:false})`; 3D enabled only when `reduced===false && isWebGLAvailable()`; `useReducedMotion` returns null until measured so WebGL never briefly mounts for reduced-motion users; static fallback always rendered → headline stays LCP), lib/webgl.ts (guarded probe, no three.js import). Independently re-ran `npm run build` → exit 0 after one transient Windows `copyfile` ENOENT flake (`_not-found.html`→`404.html`) that cleared on retry — not a code defect; won't occur on Vercel/Linux.
- [x] Staggered hero entrance reveal on preloader-complete — `@frontend-engineer` — 2026-06-10
  - Files: lib/reveal.ts (new), components/hero/HeroContent.tsx (new), components/chrome/Preloader.tsx, components/ui/CtaLink.tsx, components/sections/Landing.tsx
  - Summary: `lib/reveal.ts` is a tiny LATCHED pub/sub decoupling the root-level Preloader from the deeply-nested hero; the Preloader calls `markRevealStarted()` the moment its count completes (overlay begins clearing). `HeroContent` (holds the single <h1>/LCP) subscribes via `onReveal` and builds the GSAP timeline: eyebrow fade-up → headline line-by-line (SplitText, reverted after for clean a11y markup) → glass object wrapper scale/fade-in → sub copy → CTA underline draw (scaleX, via new `data-cta-underline` hook on CtaLink) → scroll cue. KEY LCP decision: nothing is pre-hidden before paint — the opaque preloader overlay already occludes the hero, so the headline paints early (fast LCP) and the GSAP "from" state is applied only at the reveal moment, choreographed with the overlay fade (no flash). Reduced-motion or a reveal that already fired before mount → no animation, the natural visible markup is final (instant). 5s failsafe so the hero can never stay hidden if the signal is missed.
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0. Reload → preloader 0→100, then hero lines stagger in as overlay clears; CTA underline draws L→R. Reduce-motion → hero just appears, no stagger.
  - QA: PASS — independent QA read lib/reveal.ts (latched, idempotent pub/sub; `onReveal` runs late subscribers on a microtask; 5s failsafe path noted) confirming the Preloader→hero decoupling tolerates mount-order races and the headline is never left hidden. Build exit 0 (see hero-object entry).
- [x] Per-panel entrance reveals as each becomes active — `@frontend-engineer` — 2026-06-10
  - Files: components/sections/PanelReveal.tsx (new), components/sections/About.tsx, components/sections/Systems.tsx, components/sections/Contact.tsx
  - Summary: `PanelReveal` wraps a panel's content and reveals its `[data-reveal]` elements ONCE (fade + rise, 0.9s power3.out, 0.1s stagger). Primary trigger is the deck's `activeIndex` (DeckContext) per spec; an IntersectionObserver is a deliberate SECOND trigger so native-scroll mode (mobile / reduced-motion fallback, where activeIndex isn't updated by manual scrolling) still reveals on scroll-into-view — whichever fires first, guarded to run exactly once (no re-trigger on revisit). Content is hidden via JS only (after mount, while the panel is off-screen, so invisible), keeping SSR/no-JS output fully visible. Reduced-motion → no hide, no animation (instant). Applied to About (idx 1), Systems (idx 2, incl. capability cards), Contact (idx 3, intro + form); footer intentionally left always-visible.
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0. Snap to each panel → content rises in once; snap away and back → no re-animation. Reduce-motion → content present immediately.
  - QA: PASS — independent QA read PanelReveal.tsx: `done` guard + `io.disconnect()` enforce reveal-exactly-once (no replay on revisit); deck `activeIndex` is the primary trigger with IntersectionObserver as the native-scroll fallback; reduced-motion path leaves markup visible and no-ops the watcher; pre-hide is JS-only after mount so SSR/no-JS stays visible. Build exit 0.

### Revision — hero object rebuilt (orb → abstract intelligence structure); Gate 3 not yet approved
- [x] Hero object REVISION — replace glass ORB with an abstract semi-organic glass STRUCTURE per Gabe's revised creative brief — `@frontend-engineer` — 2026-06-10
  - Deps: NO new dependencies (motion stack stays GSAP + R3F `useFrame` only — Framer Motion / framer-motion-3d explicitly NOT added, honouring the locked single-motion-library decision).
  - Files: components/hero/HeroGlass.tsx (full rewrite — geometry/material/behaviour replaced), components/hero/HeroObjectFallback.tsx (new abstract-structure SVG, brief's glass palette), components/hero/HeroContent.tsx (hero H1 + sub copy updated to the brief), components/hero/HeroObject.tsx (doc comment: orb → "intelligence structure"). REUSED AS-IS: lib/reveal.ts, lib/use-reduced-motion.ts, lib/webgl.ts, components/sections/PanelReveal.tsx, HeroObject seam (dynamic ssr:false + WebGL probe + reduced-motion gating).
  - Summary: The orb's `sphereGeometry` + clear-glass material + simple float are GONE. New object = a CPU-prewarped, asymmetric icosahedral glass shell (icosa detail 5, ~20k tris) so the silhouette is non-spherical/hard-to-categorise, rendered with drei `MeshTransmissionMaterial` (color #DDEEFF, attenuationColor #A7C8FF — soft blue glass) and GPU vertex displacement (gdg_-prefixed 3D simplex noise injected via a CHAINED `onBeforeCompile` that preserves drei's transmission shader + busts the program cache key) for living topology + awareness ripples. Inside: a second translucent shell + ~520 GPU-shaded `Points` (cloud animated entirely in the vertex shader — no CPU sim) + a fibonacci node lattice of `LineSegments` whose opacity fades in with awareness. BEHAVIOUR: idle breathing (group scale) + float + slow rotation; proximity via cursor-to-centre distance (`state.pointer`) raising `uProximity` (damped) → particles activate, surface ripples, connections appear (fine pointers only); hover (raycast over mesh) raising `uHover` → particles self-organise onto the lattice, nodes connect; SINGLE evolution axis `uStage` (0..3) GSAP-tweened 2s `power2.inOut` (organic, in the 1.5–2.5s band) — scroll (deck `activeIndex`, with a 4-panel IntersectionObserver fallback for native-scroll that only fires on a real dominant-panel change so it never reverts a click-advance) REASSERTS the panel stage [Landing→0 Potential, About→1 Formation, Systems→2 Organization/Understanding, Contact→3 Stability]; clicking the mesh advances Potential→Memory→Understanding→Intelligence; navigating reasserts. All inputs funnel into one `goToStage()` so the two systems never fight. Perf: GPU-only motion, `dpr={[1,2]}`, env resolution 128, transmission samples 6, off-screen render loop paused (`frameloop="demand"` via wrapper IntersectionObserver) while the GSAP stage tween keeps running. FALLBACKS unchanged-by-design: HeroObject still renders the static on-brand SVG for SSR/first-paint, no-WebGL AND reduced-motion (fully static — no breathing/drift/proximity/parallax/morph); WebGL probed before the heavy chunk loads; `<h1>` stays LCP (canvas dynamic ssr:false, three.js in a separate ~684 kB async chunk OUT of the route's First Load).
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0 (`/` 49.6 kB page / 137 kB First Load JS — unchanged; three.js/drei isolated in a ~684 kB async chunk). Manual: idle breathing/float, cursor-proximity awareness, hover self-organisation, click cycles 4 morph states (~2s each), scroll across the 4 panels evolves the object, OS reduce-motion → static SVG structure (no canvas), WebGL disabled → static SVG structure.
  - QA: PASS — independent QA read the full HeroGlass.tsx rewrite: confirmed NO `sphereGeometry` (prewarped asymmetric `IcosahedronGeometry` + sine-field lumping + non-uniform axis scale → non-categorisable silhouette); particles are GPU `Points` animated 100% in the vertex shader (no CPU sim); ONE `uStage` axis GSAP-tweened with scroll-primary/click-explore/IO-fallback precedence wired correctly; reduced-motion gated out upstream in HeroObject (canvas never mounts); `frameloop` drops to `demand` off-screen; only gsap/three/drei/fiber imported (no Framer Motion); object palette kept separate from the locked UI `--accent`. Independently re-ran `npm run build` → exit 0 (`/` 137 kB First Load; three.js async chunk), passed even with a dev server running. The `onBeforeCompile`-chain into drei's transmission material is the one fragile seam (flagged by the engineer; degrades gracefully — displacement no-ops, still renders) — visual to be confirmed at the gate.

### Revision #3 — persistent shared object across all 4 panels + space-window look + click scenes; Gate 3 not yet approved
- [x] Hero object REVISION #3 — make the object ONE persistent shared canvas travelling alternating halves; retire scroll "evolution stages" (scroll now drives POSITION); change look to a window-into-deep-space; proximity calm→energetic; per-panel click scenes — `@frontend-engineer` — 2026-06-10
  - Deps: NO new dependencies (motion stack stays GSAP + R3F `useFrame` only — Framer Motion explicitly NOT added). No `npx shadcn add`.
  - Files: ADDED components/hero/space/types.ts (three-free shared contracts: SCENE ids, PANEL_SEQUENCES, side-sign, SceneShared bag), components/hero/space/scene-data.ts (geometry + 8 precomputed scene layouts + meteor geometry + shared SNOISE), components/hero/space/SceneCanvas.tsx (the ONE R3F Canvas + scene), components/hero/SceneStage.tsx (lightweight DOM host: eligibility gating, dynamic ssr:false import, window pointer/click controller, scene controller), components/hero/PanelObjectSlot.tsx (per-panel reserved half + static SVG, hides when live), lib/scene-presence.ts (live-canvas pub/sub). REWROTE components/hero/HeroObjectFallback.tsx (orb/structure SVG → space-window: dark void + star glitter + nebula + glass rim on the unchanged #FAF9F6). EDITED components/hero/HeroContent.tsx (two-column: copy left, reserved object half right; removed in-column object + its reveal tween), components/sections/About.tsx (object left / philosophy right), components/sections/Systems.tsx (content left as a single-column capability list / object right), components/sections/Contact.tsx (object left / intro+form right), app/page.tsx (mount <SceneStage/> in deck chrome). DELETED components/hero/HeroObject.tsx + components/hero/HeroGlass.tsx (superseded). REUSED AS-IS: lib/reveal.ts, lib/use-reduced-motion.ts, lib/webgl.ts, PanelReveal, the deck/DeckContext.
  - Summary: The object is now ONE persistent full-viewport `pointer-events-none` WebGL overlay (single three.js context) mounted once in the deck chrome (inside DeckProvider), NOT a per-panel canvas. It reads the deck `activeIndex` and damps its world-X (R3F useFrame, ~1s eased — chosen over a GSAP tween so it auto-tracks live viewport resizes; GSAP still drives the discrete scene morphs + the entrance reveal) to the active panel's half: Landing right, About left, Systems right, Contact left; each panel reserves the OPPOSITE half (PanelObjectSlot) so content never collides. SHARED-CANVAS INTERACTIVITY (the high-risk constraint) solved WITHOUT R3F pointer events: the canvas writes the object's per-frame projected screen rect into a shared ref; a window-level pointer/click controller in SceneStage reads it to drive proximity/hover and object clicks, and IGNORES any event whose target is a real interactive element (`a,button,input,textarea,select,label,[role=button],[contenteditable]`) — so the Contact form, TopBar/SectionNav links, the CTA and keyboard nav stay fully usable while the object stays clickable. LOOK = window into deep space: premium drei `MeshTransmissionMaterial` glass shell on the unchanged light page, with a DARK void inner mesh (procedural nebula via SNOISE), ~560 GPU star points (size/twinkle/colour per-vertex attributes, additive, depthTest-off + renderOrder so they read through the glass) and faint nebula-tinted "galaxy" stars. PROXIMITY: cursor distance to the object's CURRENT screen rect raises `uProximity`/`uHover` (damped, refined — never violent), gating star drift/twinkle, surface ripple and rotation; disabled on coarse/touch pointers. CLICK SCENES funnel through ONE controller (per-panel step, RESET to idle on panel change so scenes never bleed; scroll never triggers a scene): Landing cycles METEOR→NEURON→GALAXY→BLOOM→idle; About=CONSTELLATION, Systems=LATTICE, Contact=ORBIT (one each). Particle scenes morph by swapping two precomputed target attributes (`aPrev`/`aTarget`) and GSAP-tweening `uMorph` — NO per-frame CPU sim; meteor is a separate ~20-streak clip-space `lineSegments` layer (GPU shader, sparse/elegant, page visible beneath) with the object fading (`uObjAlpha`) as it "dissolves"; galaxy/orbit spin + bloom collapse/pop are group transforms in useFrame. PERF: ONE context, `dpr={[1,2]}`, transmission samples 6 / res 256, all particles/stars/meteors GPU-driven. FALLBACKS: live canvas mounts ONLY at `lg+` AND motion-on AND WebGL AND after the preloader reveal (LCP guard); otherwise the per-panel static space-window SVG shows (covers SSR/first-paint, no-WebGL, reduced-motion, and below-lg where panels are single-column with no object). `<h1>` stays LCP — SceneStage never imports three; SceneCanvas is `dynamic(import,{ssr:false})` so three stays in its own async chunk OUT of First Load.
  - Verify: `npm run lint` clean, `npx tsc --noEmit` exit 0, `npm run build` exit 0 (`/` 50.5 kB page / 138 kB First Load JS — three.js/drei isolated in a separate async chunk, NOT in First Load). Manual (lg+ desktop, motion on, WebGL): object persists and slides to the correct half on each of the 4 panels; Contact form + TopBar/SectionNav + CTA + keyboard nav all still work with the object present; stars/nebula visible inside the glass on the light page; cursor far→near goes calm→energetic; click Landing object cycles meteor(~20 streaks top-left→down-right)→neuron→galaxy→bloom→idle; About/Systems/Contact each one themed click anim; navigating resets the scene to idle. OS reduce-motion OR WebGL off OR <lg → static space-window SVG (no canvas); below lg → single-column, no object.
  - QA: PASS (code + build) — independent QA read SceneStage.tsx: confirmed the overlay is `pointer-events-none fixed inset-0` (form/nav/CTA/keyboard stay usable) with object hover/click reconstructed from a window pointer listener vs the per-frame screen rect, `isInteractiveTarget()` bailing on real UI targets so the object never steals a form/nav click; scene state resets to idle on `activeIndex` change (scenes don't bleed); scroll only repositions; live-canvas gated on lg+ AND motion-on AND WebGL AND post-reveal (LCP guard); no three import in SceneStage. Independently re-ran `npm run build` → exit 0 (`/` 138 kB First Load; three.js async chunk). VISUAL/INTERACTION acceptance (stars reading through glass, meteor/neuron/galaxy/bloom + per-section scene aesthetics, true 60fps, Contact-form-with-object feel) deferred to Gabe at the Human Test Gate — not certifiable by static review.

### Removal — hero 3D object scrapped entirely (Gabe's direction); redesign to follow page-by-page
- [x] REMOVE the persistent WebGL hero object ("the blob") and all supporting code/deps — `@frontend-engineer` — 2026-06-10
  - Rationale: Gabe rejected the hero object outright ("remove any and all traces of that shape, the blob, I want that gone") after Gate 3 visual review. The space-window concept and all prior hero-object revisions (orb → intelligence structure → persistent space-window) are retired. We will redesign the site one page at a time, starting with Landing — that redesign is a separate, future task. This entry is the clean teardown only.
  - Files DELETED: components/hero/SceneStage.tsx, components/hero/space/SceneCanvas.tsx, components/hero/space/scene-data.ts, components/hero/space/types.ts (+ the components/hero/space/ dir), components/hero/PanelObjectSlot.tsx, components/hero/HeroObjectFallback.tsx, lib/scene-presence.ts, lib/webgl.ts. (components/hero/ now holds only HeroContent.tsx.)
  - Files EDITED: app/page.tsx (dropped `<SceneStage/>` + import + chrome comment), components/hero/HeroContent.tsx (removed PanelObjectSlot import/usage; collapsed the lg:grid-cols-2 copy/object split to a single max-w-2xl column; kept the SplitText entrance reveal, h1/sub/CTA/scroll-cue untouched), components/sections/About.tsx + Systems.tsx + Contact.tsx (removed PanelObjectSlot import/usage; collapsed each two-column object/content grid to a single centered column — interim layout pending per-page redesign), components/sections/Landing.tsx (doc comment only).
  - Deps PRUNED (package.json): removed `three`, `@react-three/fiber`, `@react-three/drei` (deps) and `@types/three` (devDep); kept `gsap` (still used by the hero reveal + deck). `npm install` removed 67 packages.
  - Verify: `npx tsc --noEmit` exit 0; `npm run lint` clean (no dangling imports/unused vars); grep for SceneStage/PanelObjectSlot/HeroObjectFallback/scene-presence/SceneCanvas/isWebGLAvailable/@react-three/three across app|components|lib → no matches. `npm run build` could NOT be run in-session — a `next dev` server held a lock on .next/trace (EPERM); no code issue (tsc+lint green). Build to be run after the dev server is stopped.
  - QA: PASS — dev server stopped + `.next` cleared 2026-06-10; baseline `npm run build` exit 0 (compiled successfully, `/` 47.7 kB / 135 kB First Load JS, no three.js chunk present). Post-removal tree confirmed clean before Phase 3R begins.

## Phase 3R — Cinematic Landing redesign: particle-tunnel hero + directional transitions
- [x] Re-add the pinned React-18-line 3D core; keep gsap. — `@frontend-engineer` — 2026-06-10
  - Files: package.json, package-lock.json
  - Summary: Re-added the pinned minimal 3D core with `--save-exact` so versions are locked, not caret: `three@0.169.0`, `@react-three/fiber@8.18.0`, `@react-three/drei@9.122.0` (deps) + `@types/three@0.169.0` (devDep). `gsap@^3.15.0` retained (motion + deck). `npm install` added 67 packages. No framer-motion / @react-spring (GSAP-only lock honoured); no `npx shadcn add`.
  - Verify: package.json shows exact pins (no `^`); `npm run build` exit 0 with the three stack isolated in a 668 kB async chunk (`b536a0f1.*.js`), NOT in the route's 137 kB First Load JS.
  - QA: PASS
- [x] Dark Landing theming (token scope + adaptive chrome + dark preloader). — `@frontend-engineer` — 2026-06-10
  - Files: app/globals.css, components/sections/Panel.tsx, components/sections/Landing.tsx, components/chrome/TopBar.tsx, components/chrome/SectionNav.tsx, components/chrome/Preloader.tsx
  - Summary: Added a `[data-theme="dark"]` scope in globals.css re-valuing the SAME five semantic tokens (bg #06070a, ink #eaecf2, muted #8b93a7, hairline light-alpha, accent #6ea8ff) — no hard-coded hex in components; everything inside keeps using `text-ink`/`bg-bg`/etc. and inherits. `Panel` gained `theme?: 'dark'` (sets `data-theme`) and `transparent?` (Landing is transparent so the backdrop shows through; other panels solid `bg-bg` to occlude it and read as a clean wipe). Landing opts into `theme="dark" transparent`. TopBar `<header>` + SectionNav `<nav>` opt into `data-theme="dark"` when `activeIndex === 0` (custom props inherit even to fixed chrome), with `transition-colors` so the swap crossfades; all chrome colours were already token-based. Preloader root got `data-theme="dark"` (dark bg + light count) for a seamless reveal into the tunnel.
  - Verify: tsc + lint clean; build exit 0. Token swap is class-only (no hex in components); About/Systems/Contact untouched on the light palette.
  - QA: PASS (code/build); dark look + chrome legibility on both palettes is a Gate 3R visual item.
- [x] Particle tunnel (probe + host + canvas + grain + static backdrop). — `@frontend-engineer` — 2026-06-10
  - Files: lib/webgl.ts (recreate), components/hero/tunnel/TunnelStage.tsx (new), components/hero/tunnel/TunnelCanvas.tsx (new), components/hero/tunnel/Grain.tsx (new), components/hero/HeroBackdropFallback.tsx (new), app/globals.css (grain keyframes)
  - Summary: `lib/webgl.ts` recreated as a generic WebGL probe (no three import). `TunnelStage` is a light DOM host (no three import) that always renders the static dark backdrop + animated grain and ONLY mounts the heavy canvas via `dynamic(() => import('./TunnelCanvas'), { ssr:false })` when ALL gates pass — lg+ (≥1024) AND motion-on AND WebGL AND post-`onReveal` (LCP guard); fixed `inset-0 pointer-events-none z-0`; opacity tied to `activeIndex` (1 on Landing, crossfade to 0 on exit). `TunnelCanvas` is the R3F `Canvas`: ~1400 GPU `Points` on a cylinder wall flowing toward the camera and recycling (mod over depth) for an endless tunnel; ALL motion in the vertex/fragment shaders (size attenuation, fade in far / out near, twinkle, additive in-shader glow); cursor drift + proximity energy read from a WINDOW pointer listener (canvas is pointer-events-none) and damped (calm→energetic, never violent), disabled on coarse pointers; `dpr={[1,2]}`, `frameloop` drops to `never` when off Landing (no GPU cost), drei `AdaptiveDpr`, geometry/material disposed on unmount. `Grain` = tiled inline-SVG feTurbulence overlay, `mix-blend-soft-light`, animated via `gdg-grain-shift` keyframes (frozen by the global reduced-motion guard). `HeroBackdropFallback` = static layered dark-gradient nebula + static grain (decorative artwork colours, not tokens).
  - Verify: tsc + lint clean; build exit 0; three stack confirmed in the 668 kB async chunk, OUT of First Load. No `@react-three/postprocessing` (overlay grain + in-shader glow per spec).
  - QA: PASS (code/build); tunnel look / grain intensity / 60fps are Gate 3R visual items.
- [x] Directional transition engine (PanelDeck rework — reopens Phase 2). — `@frontend-engineer` — 2026-06-10
  - Files: components/deck/PanelDeck.tsx, components/deck/DeckContext.tsx, app/page.tsx
  - Summary: Replaced deck-mode's single vertical `yPercent` track with ABSOLUTELY-STACKED panels (`absolute inset-0` each) inside `main#content` (skip-link target preserved). A `PAIR` map drives per-adjacent-pair direction — `0-1` y/+ (down), `1-2` x/+ (side), `2-3` y/− (up); `goToPanel()` builds a GSAP timeline placing the incoming panel at its enter offset and sliding it to 0 while the outgoing exits the opposite way (`power2.inOut`, 0.9s). Non-adjacent jumps (nav/deep-link, e.g. 0→3) animate as fast CHAINED legs (each reads in its own direction) with total duration capped at `JUMP_CAP` 1.15s. PRESERVED unchanged: Observer (`onUp`/`onDown` → `goToPanel(±1)`, 1 gesture = 1 panel), `goToPanel` single entry, `lockedRef` input-lock (+ timer fail-safe), keyboard (Arrow/Page/Home/End/Space), hash `replaceState` deep-links + popstate, focus mgmt (skips form fields), clamping (no wrap), resize/orientation rebuild across the breakpoint, live reduce-motion switching, and the native-scroll fallback (deck disabled, `scrollIntoView`). `PanelDeck` API changed from `children` to `panels: ReactNode[]` + `backdrop?`; DeckContext now also exposes `deckMode` (so the backdrop knows native vs deck). app/page.tsx passes the 4 panels + `<TunnelStage/>` backdrop + chrome.
  - Verify: tsc + lint clean; build exit 0 (`/` static). NO `scroll-snap-type` in source or built CSS (Observer deck only). Active state committed at transition START so chrome + tunnel opacity crossfade in sync.
  - QA: PASS (code/build); transition feel/direction + one-gesture-one-panel + non-adjacent jumps are Gate 3R items (incl. real iOS/Android).
- [x] Landing hero content (dark/minimal, legible over tunnel, mount host). — `@frontend-engineer` — 2026-06-10
  - Files: components/hero/HeroContent.tsx, app/page.tsx
  - Summary: Kept the single `<h1>` (LCP), sub, CTA, scroll cue and the existing SplitText `onReveal` entrance timeline untouched. Added a faint left-anchored radial scrim behind the copy + subtle `text-shadow` on the headline/sub for legibility over the brightest tunnel particles (no full-panel darkening). Tokens (now dark-scoped) render the copy light automatically. The tunnel host is mounted once in the deck chrome via app/page.tsx (`backdrop` prop), behind the transparent Landing.
  - Verify: tsc + lint clean; build exit 0; `<h1>` unchanged (still the LCP element; canvas mounts post-reveal in its own chunk).
  - QA: PASS (code/build); contrast over the live tunnel is a Gate 3R visual item.
- [x] Fallbacks (reduced-motion / mobile / no-WebGL / pre-reveal). — `@frontend-engineer` — 2026-06-10
  - Files: components/hero/tunnel/TunnelStage.tsx, components/deck/PanelDeck.tsx, components/deck/DeckContext.tsx (deckMode)
  - Summary: Correctness paths verified end-to-end. Canvas mounts ONLY when reduced===false AND lg+ AND WebGL AND post-reveal; otherwise the static `HeroBackdropFallback` (dark gradient + static grain) is the Landing backdrop — covering SSR/first-paint, no-WebGL, reduced-motion, below-lg, and pre-reveal. PanelDeck native mode (reduced-motion OR <768px) keeps panels in document flow with native scroll, instant `goToPanel` jumps, no lock, no directional animation. Fixed a native-mode bug: `activeIndex` isn't updated by manual scroll, so the transparent Landing could expose the light body bg if opacity were tied to it — TunnelStage now reads `deckMode` from context and keeps the static backdrop fully opaque in native mode (opaque light panels cover it when off Landing). Grain animation is frozen by the global reduced-motion CSS guard.
  - Verify: tsc + lint clean; build exit 0. Logic-reviewed: every fallback gate resolves to static-backdrop + native-scroll with no canvas.
  - QA: PASS (code/build); real reduced-motion / mobile / no-WebGL device behaviour is a Gate 3R item.

### Change — palette decision reversed: dark-Landing/light-rest → FULL-DARK site (tunnel stays Landing-only)
- [x] Full-dark reversal — `@frontend-engineer` — 2026-06-10
  - Rationale: Gabe reversed the Phase 3R palette decision after the first gate presentation — "Dark bg everywhere, tunnel stays on Landing." About/Systems/Contact now show the same dark atmospheric gradient + grain (NO particles) on the dark token scope; only the WebGL particle tunnel remains a Landing-only centerpiece.
  - Files: app/globals.css (`color-scheme:dark` on the dark scope + on `html`; comments), app/layout.tsx (`data-theme="dark"` on `<body>` → site-wide dark base/scope, no light flash), components/sections/About.tsx + Systems.tsx + Contact.tsx (`theme="dark" transparent` so the shared backdrop shows through and content uses the dark scope), components/chrome/TopBar.tsx + SectionNav.tsx (dark on ALL panels, not just `activeIndex===0`), components/hero/tunnel/TunnelStage.tsx (SPLIT: gradient+grain now opacity 1 site-wide; ONLY the WebGL canvas wrapper fades by `activeIndex` so particles stay Landing-only; removed the host-level activeIndex/deckMode opacity gating), components/hero/tunnel/TunnelCanvas.tsx (resting motion bumped: COUNT 1400→1900, base flow speed 1.1→1.8, resting alpha 0.45→0.62 — modest, tasteful, flagged LIVE-TUNABLE in code).
  - Summary: Token-pure — the same five semantic tokens re-valued by the dark scope, now applied site-wide via `<body data-theme="dark">`; every panel is `transparent` over one continuous site-wide dark gradient+grain backdrop, so directional transitions slide content over an unbroken dark field (no light flash between sections). Chrome carries `data-theme="dark"` explicitly on all panels. `color-scheme:dark` makes the Contact form fields/disabled button + scrollbars/caret render dark; the Systems capability list (hairline borders, accent numerals) and Contact form are token-based and flip to legible light-on-dark automatically. The particle tunnel is unchanged in placement/gating (lg+ + motion + WebGL + post-reveal, dynamic ssr:false) — only its brightness/density/speed at rest were raised so it clearly reads as motion. PanelDeck directional engine untouched this round. Re: the "where's the animation" report — the tunnel renders on desktop ≥1024 with WebGL + motion-on once the preloader reveal fires; the boost addresses "too subtle at rest."
  - Verify: `npx tsc --noEmit` exit 0; `npm run lint` clean; `npm run build` exit 0 (`/` 49.9 kB / 137 kB First Load JS; three.js/R3F/drei still isolated in the 668 kB async chunk `b536a0f1.*.js`, NOT in First Load; `<h1>` stays LCP). One transient Windows webpack chunk-race build failure (`Cannot find module './159.js'` during `/_not-found` page-data collection) cleared on a clean-`.next` retry — not a code defect (compile + type-check had already passed); won't occur on Vercel/Linux. No `scroll-snap-type` in source/built CSS.
  - QA: PASS (code/build). Full-dark look, no-light-flash transitions, tunnel brightness/density, and Contact-form/Systems-list legibility on dark are Gate 3R visual items for Gabe.

## Phase 3R.1 — Landing tunnel feel (cinematic lighting)
> Prototyped directly in the main session at Gabe's request; all changes confined to `components/hero/tunnel/TunnelCanvas.tsx` (shaders + geometry attributes). Gate 3R.1 remains a Gabe visual item (not formally approved).
- [x] Speed control — constant slow base + small bounded proximity lift — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/hero/tunnel/TunnelCanvas.tsx
  - Summary: Particle flow speed is a constant per-particle base (`0.5 + aRand*0.55`) so it NEVER depends on elapsed time and cannot creep up over a long dwell (fixes the original runaway-speed bug where sitting on Landing made particles fast). A SMALL hard-bounded cursor-proximity lift (`uProximity*0.3`, ≤~30% over base — vs the old runaway `uProximity*2.6`) eases the flow up near screen-centre and adds a brightness glow; the time-varying part is integrated into `uFlow` on the CPU so a changing speed never teleports particles.
  - Verify: `npm run typecheck` exit 0, `npm run lint` clean, `npm run build` exit 0; headless WebGL probe = no shader errors; speed visibly constant across a long dwell, cursor-centre boost subtle and bounded.
  - QA: PASS (code/build/WebGL-probe); subjective "feel" of the speed is a Gate 3R.1 visual item.
- [x] Cinematic colour flashing — per-particle palette drift — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/hero/tunnel/TunnelCanvas.tsx
  - Summary: Each particle drifts slowly through a cool cinematic cosine palette (blue → teal → indigo → white), phase-offset by its per-particle random (`vRand`) so the colours shimmer scattered across the field rather than pulsing in unison. Subtle, GPU-only (fragment shader), no CPU work.
  - Verify: tsc/lint/build clean; WebGL probe no shader errors; colours shift over time, scattered not synchronised.
  - QA: PASS (code/build/WebGL-probe); look is a Gate 3R.1 visual item.
- [x] Occasional coordinated sparks between neighbours — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/hero/tunnel/TunnelCanvas.tsx (added `aGroup` geometry attribute + spark term in FRAG)
  - Summary: Particles are binned into small spatial clusters via a new `aGroup` attribute (`angular sector × depth slice`). Each cluster rolls on its own desynced clock; ~10% fire per cycle (`step(0.90, roll)`), lighting their neighbours bright together for a brief instant (quick `exp(-fract(t)*7.0)` decay) → a spark between neighbours. Sparse and quick, never constant.
  - Verify: tsc/lint/build clean; WebGL probe no shader errors; sparks fire occasionally between neighbouring particles, then fade.
  - QA: PASS (code/build/WebGL-probe); spark frequency/aesthetics are a Gate 3R.1 visual item.
- NOTE: this work also fixed the shader LINK error (the "runtime error" Gabe asked me to chase) — FRAG `precision mediump float;` → `highp` to match VERT for the shared `uTime`/`uProximity` uniforms; the precision mismatch had failed `VALIDATE_STATUS` so the tunnel never rendered (thousands of `drawArrays: no valid shader program` warnings). Verified 0 INVALID_OPERATION via headless Chrome after the fix.

## Phase 3R.2 — "Dive into the void" warp transition
> Prototyped directly in the main session at Gabe's request. Gate 3R.2 remains a Gabe visual item (not formally approved). NOTE: originally scoped to the Landing↔About (0↔1) leg only; later GENERALIZED to every leg by the 2026-06-10 structure restructure (see the structure-update entry below).
- [x] Shared transition signal (`lib/warp.ts`) — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: lib/warp.ts (new), components/deck/PanelDeck.tsx
  - Summary: A plain mutable singleton (NOT React state) holding a 0→1 "dive progress" scalar, scrubbed by a proxy tween inside the deck's transition timeline and read every frame by the tunnel's R3F render loop (no re-renders). Mirrors the latched style of `lib/reveal.ts` but is a continuously-scrubbed scalar. Convention was "0 = Landing rest, 1 = at About"; later generalized to "0 at rest, ramps 0→1 across any transition".
  - Verify: tsc/lint/build clean; WebGL probe confirms the tunnel reads the scrub each frame (warp visible mid-transition).
  - QA: PASS (code/build/WebGL-probe).
- [x] Tunnel warp PULSE + site-wide persistence — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/hero/tunnel/TunnelCanvas.tsx, components/hero/tunnel/TunnelStage.tsx
  - Summary: `uWarp = sin(dive·π)` — particles light-speed and streak radially mid-dive (`vWarp` stretches the point sprite along `vDir`), calm (0) at BOTH ends, so the tunnel never warps on load and is never stuck at light-speed on inner pages. `TunnelStage` was changed to keep the canvas full-opacity and ALWAYS running on every page (removed the active-index opacity/frameloop crossfade) → particles are now SITE-WIDE (this REVERSES the earlier "tunnel Landing-only" decision, at Gabe's clarification that "the hero" = the particle animation, not the headline text). Flow is CPU-integrated (`uFlow`) so the speed change never teleports particles.
  - Verify: tsc/lint/build clean (First Load 138 kB, three.js async-isolated); WebGL probe = no shader errors, warp pulses then calms.
  - QA: PASS (code/build/WebGL-probe); warp intensity + inner-page legibility are Gate 3R.2 visual items.
- [x] Deck timeline rework — staggered panel hand-off — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/deck/PanelDeck.tsx
  - Summary: Replaced the vertical `yPercent` slide with a STAGGERED hand-off (`addDiveLeg`): outgoing panel scales up + fades out over the first ~55% as the warp builds; incoming fades in over the last ~60% as it settles — so the two panels never overlap at full opacity (fixes the muddy double-headline the first build showed). `parkPanels` resets scale + autoAlpha (not just translate) so the dive treatment never leaves a panel stuck. Observer one-gesture-one-panel, `goToPanel` single entry, input lock, keyboard, hash deep-links, and focus mgmt all preserved.
  - Verify: tsc/lint/build clean; headless probe confirms mid-dive the outgoing panel is hidden while the incoming fades in (no double-exposure).
  - QA: PASS (code/build/probe); transition feel is a Gate 3R.2 visual item.
- [x] Fallbacks (reduced-motion / mobile / no-WebGL / pre-reveal) — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: components/deck/PanelDeck.tsx, components/hero/tunnel/TunnelStage.tsx
  - Summary: In native mode (reduced-motion OR <768px) and pre-reveal / no-WebGL, there is no warp and no canvas — plain instant section change + native scroll, with the static dark backdrop standing in. `setDive` is reset to rest (0) on every native/immediate/init path so a stale dive can never warp on load.
  - Verify: tsc/lint/build clean; logic-reviewed every fallback gate resolves to static-backdrop + native scroll + no warp.
  - QA: PASS (code/build); real reduced-motion/mobile/no-WebGL device behaviour is a Gate 3R.2 item.

### Structure update — three-panel restructure + warp dive on every leg + hero social marks (Gabe, in-session)
- [x] Remove About; rename Landing→Welcome and Systems→Projects (placeholder); warp dive on EVERY leg; add GitHub + LinkedIn hero marks — `@frontend-engineer` (in-session) — 2026-06-10
  - Files: lib/site-config.ts (3 panels; SYSTEMS_LABEL 'Systems'→'Projects'; PanelId drops 'about'), app/page.tsx (drop About; 3 panels; doc), components/sections/About.tsx (DELETED), components/sections/Systems.tsx (Projects placeholder "Selected work, coming soon." + In-progress indicator; PanelReveal idx 2→1; marker 03→02), components/sections/Contact.tsx (PanelReveal idx 3→2; marker 04→03), components/sections/Landing.tsx (ariaLabel Landing→Welcome), components/hero/HeroContent.tsx (GitHub+LinkedIn social marks under the sub-line, wired into the reveal), components/deck/PanelDeck.tsx (generalized `addDiveLeg` to any pair; every leg routes to the dive; removed the directional-slide engine — PAIR/legGeometry/axisProp/EASE; `setDive` convention now 0-at-rest), components/hero/tunnel/TunnelCanvas.tsx + lib/warp.ts (doc comments updated to the new convention), README.md + CLAUDE.md (docs brought in line with the 3-panel structure).
  - Summary: Per Gabe's request the site went from 4 panels to 3 — About removed, "Landing"→"Welcome", "Systems"→"Projects" (a placeholder until the work showcase is designed). The cinematic warp dive (3R.2) is now the transition on EVERY adjacent leg (Welcome↔Projects, Projects↔Contact) in both directions, via a generalized `addDiveLeg`; the per-pair directional-slide engine was removed. The dive-signal convention changed to "0 at rest, 0→1 pulse per leg" (symmetric `sin(dive·π)`). Themed monochrome GitHub (github.com/GDGuzman11) + LinkedIn (linkedin.com/in/gabe-de-guzman) marks were added under the hero sub-line. Internal ids/hashes unchanged (`#home`/`#systems`/`#contact`); "Projects" still deep-links at `/#systems`.
  - Verify: `npm run typecheck` exit 0, `npm run lint` clean, `npm run build` exit 0 (`/` 138 kB First Load; three.js still async-isolated). Headless WebGL render (puppeteer, swiftshader): nav reads `Welcome | Projects | Contact` with `aria-current` tracking; both social links resolve; warp confirmed on BOTH legs with a clean staggered hand-off (no double-exposure); `/#systems` deep-link lands on Projects; ZERO shader/WebGL/page errors. Committed `41bbb1a` and pushed to `origin/main` (https://github.com/GDGuzman11/GDGQuantumWeb).
  - QA: PASS (code/build/WebGL-probe). NOTE: Gabe-driven direct change outside the agent/gate flow; visual/interaction + real-device acceptance remains a Gabe item. WATCH (carried from 3R.2): particle legibility behind the Contact form / Projects content on real displays — flag if a subtle scrim or dimmer particles are needed.

## Phase 4 — Backend: data + contact pipeline (IN PROGRESS — code built, runtime verify pending Gabe's `.env`)
> DB choice CONFIRMED Postgres + Prisma + Resend (Gabe weighed Firebase/Firestore 2026-06-10 and kept the planned stack; provider = Neon recommended). Hosting stays Vercel. All code below builds clean (tsc 0, lint 0, `npm run build` exit 0; First Load 165 kB — RHF+zod now in the bundle, Phase 6 code-split candidate; three.js still async-isolated). The remaining steps (migration + live email/persist test) are BLOCKED on Gabe creating `.env` (Neon pooled+direct URLs + Resend key/owner). See the CLAUDE.md §0 "PHASE 4 RESUME POINT".
- [x] Shared Zod schema + React Hook Form wired client-side — `@frontend-engineer`/`@backend-engineer` (in-session) — 2026-06-10
  - Files: lib/schema.ts (new), components/sections/ContactForm.tsx (new), components/ui/Field.tsx (RHF-ready rewrite), components/sections/Contact.tsx (swap visual form → <ContactForm/>), package.json (+zod, react-hook-form, @hookform/resolvers)
  - Summary: `lib/schema.ts` is the SINGLE shared contact schema (name/email/message; trim+lowercase+regex email, length caps) consumed by BOTH the client (RHF via `zodResolver`) and the server action (re-validates). `ContactForm` is a client component with inline field errors, an `isSubmitting` state, and in-place success/error rendering (no navigation away); server-side field errors are mapped back onto inputs. `Field` was made RHF-ready (forwardRef + spreads `register` + accessible `aria-invalid`/`aria-describedby` error).
  - Verify: `npm run typecheck` 0, `npm run lint` 0, `npm run build` exit 0 (`/` 165 kB First Load). Client-side validation works with no DB (independent of creds).
  - QA: PASS (code/build).
- [ ] Prisma ContactSubmission model + migration; pooled + direct URLs — `@backend-engineer` (in-session) — 2026-06-10 — MODEL BUILT / MIGRATION PENDING
  - Files: prisma/schema.prisma (new), lib/db.ts (new), package.json (prisma@6 + @prisma/client@6 + `postinstall: prisma generate`)
  - Summary: Prisma 6 (DOWNGRADED from the auto-installed Prisma 7, which dropped `url`/`directUrl` from the schema in favour of a driver-adapter + prisma.config.ts — unnecessary for our pooled-URL serverless pattern). `ContactSubmission` model (cuid id, name/email/message, nullable ipHash/userAgent for Phase 5, createdAt, @@index createdAt). Datasource uses pooled `DATABASE_URL` + `directUrl = DIRECT_URL`. `lib/db.ts` is a globalThis-cached singleton (dev hot-reload safe). Client generated.
  - Verify: `npx prisma generate` OK (client v6.19.3). tsc/lint/build clean. PENDING: `npx prisma migrate dev --name init` to create the table — needs Gabe's `.env`.
  - QA: PASS (code/build/generate); migration + table existence pending `.env`.
- [ ] Server Action (validate → security placeholder → persist → email → typed result) — `@backend-engineer` (in-session) — 2026-06-10 — CODE BUILT / RUNTIME PENDING
  - Files: app/actions/contact.ts (new), lib/email.ts (new), package.json (+resend)
  - Summary: `submitContact` re-validates with the shared schema, has a clearly-marked Phase 5 SECURITY GATE PLACEHOLDER, persists via Prisma, then sends owner + sender emails via Resend, returning a typed `ContactResult` (with fieldErrors on validation failure). `lib/email.ts` HTML-escapes user values and degrades gracefully (`sent:false`, no throw) if RESEND_API_KEY/CONTACT_OWNER_EMAIL are unset, so persistence still succeeds; an email failure after persist is logged, not surfaced to the user (row already saved).
  - Verify: tsc/lint/build clean. PENDING live verify: real submission persists a row AND both emails arrive — needs Gabe's `.env` (Resend test mode only delivers to the signup email).
  - QA: PASS (code/build); persist + dual-email delivery are the Phase 4 Human Test Gate items (need `.env`).
- NEXT (for the PM / production lead): once Gabe says `.env` is set → run `npx prisma migrate dev --name init` → `npm run dev` → submit the Contact form → confirm a DB row + both emails (Human Test Gate 4) → present to Gabe → on approval tick the remaining Phase 4 boxes + record the gate here, then STOP for Phase 5.

### ✅ GATE APPROVED — Phases 3R / 3R.1 / 3R.2 — approved by Gabe — 2026-06-10
- QA result: PASS (code/build/WebGL-probe throughout; visual/interaction signed off by Gabe)
- Notes: Approved as the consolidated current build — cinematic dark landing with the site-wide GPU particle tunnel (constant slow flow + bounded proximity lift + cinematic colour drift + coordinated sparks) and the light-speed "dive into the void" warp now on EVERY leg (Welcome↔Projects, Projects↔Contact). Includes the three-panel restructure (About removed; Landing→Welcome; Systems→Projects placeholder) and the hero GitHub/LinkedIn social marks. Gates 3R and 3R.2's original checklists referenced the prior 4-panel/directional/Landing-only model; approval is against the CURRENT 3-panel/warp-every-leg/site-wide-tunnel build (see the §0 Status block + the ▶ Current state notes in CLAUDE.md). Carried watch-item: particle legibility behind the Contact form / Projects content on real displays — revisit in Phase 6 polish if needed. Next: Phase 4 — Backend (data + contact pipeline).

